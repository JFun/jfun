{
  "name": "quarter",
  "private": true,
  "version": "0.1.0",
  "type": "commonjs",
  "description": "Quarter (Tilt 2) — tap left or right to turn the whole world a quarter at a time; everything tumbles at once. Marble-and-hole soul, a discrete rotation verb, and an on-device exhaustive solver.",
  "dependencies": {
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.4.0"
  },
  "scripts": {
    "test": "bash scripts/dev/test.sh",
    "serve": "python3 -m http.server 8783 --directory web",
    "verify": "node scripts/dev/verify.cjs"
  }
}
