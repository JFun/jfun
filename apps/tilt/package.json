{
  "name": "tilt",
  "private": true,
  "version": "0.1.0",
  "description": "Tilt — swipe to tilt the tray; every marble rolls at once into its matching hole, revealing a pixel creature.",
  "dependencies": {
    "@jfun/web-game-core": "*",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.4.0"
  },
  "devDependencies": {
    "@jfun/test-harness": "*"
  },
  "scripts": {
    "test": "bash scripts/dev/test.sh",
    "serve": "python3 -m http.server 8782 --directory web",
    "ios": "bash scripts/dev/deploy_ios.sh"
  }
}
