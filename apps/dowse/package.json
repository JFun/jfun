{
  "name": "dowse",
  "private": true,
  "version": "0.1.0",
  "description": "Dowse — felt-tray tilt deduction: invisible pegs, every clack is a clue you paid a move for.",
  "dependencies": {
    "@jfun/web-game-core": "*",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.4.0"
  },
  "devDependencies": {
    "@jfun/test-harness": "*"
  },
  "scripts": {
    "test": "bash scripts/dev/test.sh",
    "serve": "python3 -m http.server 8781 --directory web",
    "ios": "bash scripts/dev/deploy_ios.sh"
  }
}
