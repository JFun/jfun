# <Project> — PRD

## §0 — Complexity budget (mandatory, read every edit)
The hard ceiling. One mechanic; the twist is felt, not computed; tutorial = one
sentence. List the systems in play and justify each — if it's more than
"mechanic + one twist", cut. (Vinegrow/Thawline died here.)

## §1 — The core loop (moment-to-moment)
The one verb, the win condition, the failure/retry, the juice.

## §2 — Determinism & solvability contract
Pure, seedable engine: same seed → identical instance (golden-pinned). Every
instance solver-proven solvable with a known par. Never ship an unsolvable one.

## §3 — Content (MVP)
Hand-made instances vs generator; how many; the progression.

## §4 — The growth loop (NOT optional — handbook/03)
Daily (scarcity, one attempt, streak) · spoiler-free share card (ownable, carries
the play link, A/B variants) · the instant-play link · the k-funnel events.
Wired via @studio/growth-loop.

## §5 — Shell / platform
Web-first (no-build) → Capacitor iOS (@studio/native-shell). Analytics + audio
via @studio/* packages.
