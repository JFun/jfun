import C from "./src/web-game-core.js";
export const { EMPTY, FILLED, WALL, makeRNG, rngInt, pick, shuffle, makeGrid, cloneGrid, key, fromKey, fullLines, bfsSolve, solvable, VERSION } = C;
export default C;
